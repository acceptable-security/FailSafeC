Processing this file will not terminate.
Try aborting processing with Ctrl-C.
It should not leave any temporary file in /tmp,
unless --keep-temp-dir is given.

This string should not be copied to output, because of
premature termination.

<% while(1) { sleep(500000); } %>
